{% extends 'base.html.twig' %}
{% block stylesheet %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
{% endblock %}
{% block body %}
        <div class="am_formulario jg_ocultar_campos">
            <h2 class="jg_titulos">Edita tu direccion</h2>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_errors(form) }}
            {{ form_end(form) }}
        </div>

{% endblock %}