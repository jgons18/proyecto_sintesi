{% extends 'base.html.twig' %}

{% block body %}

    <div>
        <h1>Añadir nuevo producto</h1>
        {{form_start(form)}}
        {{ form_widget(form) }}
        {{ form_row(form.nameproduct) }}
        {{ form_row(form.unitprice) }}
        {{ form_row(form.description) }}
        <br>
        <!--<input type="submit" class="btn btn-success" value="Crear post">-->
        <a class="btn btn-danger" href="{{ path('app_frutas') }}">Cancelar</a></button>
        {{ form_errors(form) }}

        {{ form_end(form) }}
    </div>

{% endblock %}
