{% extends 'base.html.twig' %}

{% block body %}

    <h2>Frutas</h2>
    {% if app.user %}
        <p><a href="{{ path('new_post') }}">+ Añadir nuevo producto</a></p>
    {% endif %}
    {% for product in products %}
        <div>
            <div>
                <h3>{{ product.nameproduct }}</h3>

            </div>
        </div>
    {% endfor %}

{% endblock %}

